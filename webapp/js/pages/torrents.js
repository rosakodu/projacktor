const TorrentsPage = {
    // Left as a placeholder, logic moved to inline detail page for better UX on Steam Deck
    render(container) {
        container.innerHTML = '<h2>Torrents module</h2>';
    }
};
